# Test-only dependencies. Separate from requirements.txt so they stay out of the
# runtime image.
#
# They used to live in requirements.txt on purpose: the deployed API ran its own
# suite via POST /api/v1/test-runs, so pytest had to be installed in the image.
# That endpoint is gone, and the Dockerfile no longer copies tests/ either.
#
# CI installs this alongside requirements.txt and scripts/requirements.txt — see
# .github/workflows/test.yml. httpx is here rather than above because the app
# itself never imports it; only the ASGI test client does.
pytest==8.3.0
pytest-asyncio==0.23.8
httpx==0.27.0
