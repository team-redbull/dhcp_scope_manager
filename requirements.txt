fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.8.0
pydantic-settings==2.4.0
python-multipart==0.0.9
# Only needed for DHCP_TRANSPORT=psrp (API on Linux, DHCP server on Windows).
# Imported lazily, so the local transport runs without it.
# Pre-1.0 — pin exactly and bump deliberately.
#
# The [credssp] extra pulls requests-credssp, required for WINRM_AUTH=credssp.
# That is not optional in practice: it is the only auth mode that delegates a
# credential to the DHCP server, and without delegation every failover cmdlet
# in CLAUDE.md §6/§7 fails against the partner. Pure Python — unlike the
# [kerberos] extra, it needs no compiler or krb5 headers at image build.
pypsrp[credssp]==0.9.1
pytest==8.3.0
pytest-asyncio==0.23.8
httpx==0.27.0
